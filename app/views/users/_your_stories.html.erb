<%= javascript_include_tag 'your_stories' %>

<% if story == "None, yet"%>
<h3 id='user-story-full'>None, yet</h3>
<!-- <%=link_to("details", story_path(story), id: 'next-user-story') %>&nbsp;&nbsp;|&nbsp;&nbsp;<%=link_to("delete", story_path(story), :id => "delete-your-story", :method => :delete, data: {confirm: "Are you sure you want to delete this?"}) %> -->
<%=link_to("", '#', id: 'next-user-story') %>&nbsp;&nbsp;&nbsp;&nbsp;<%=link_to("", "#", :id => "delete-your-story", :method => :delete, data: {confirm: "Are you sure you want to delete this?"}) %>

<% else %>
  <h3 id='user-story-full'><%=story.full_story%></h3>
  <%=link_to("details", story_path(story), id: 'next-user-story') %>&nbsp;&nbsp;|&nbsp;&nbsp;<%=link_to("delete", story_path(story), :id => "delete-your-story", :method => :delete, data: {confirm: "Are you sure you want to delete this?"}) %>

  <br>

  <button type="button" id="your-stories-button">NEXT</button>
<% end %>
